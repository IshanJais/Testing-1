#include <stdio.h>
#include <math.h>
int main()
{
    int a,b;
    printf("Enter the number:  ");
    scanf("%d", &a);
    b = (sqrt(a));
    printf("\nThe square root of this number is %d", b);
}